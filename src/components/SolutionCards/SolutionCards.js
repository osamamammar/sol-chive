import React from "react";
import FilterCards from "../FilterCards/FilterCards";
import { SectionContainer } from "./SolutionCards.styles";

const SolutionCards = () => {
  return (
    <SectionContainer>
      <FilterCards></FilterCards>
    </SectionContainer>
  );
};

export default SolutionCards;
